const config = {
    APP_MAME: "Task Manager",
    APP_CREATOR_NAME: "Amin Hasanzadeh",
    DISPLAY_NAME: "Welcome Back!",
    API_NAME: undefined, //adding this as  API dynamic path for now we have no APIs
}

export default config